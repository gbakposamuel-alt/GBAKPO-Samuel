const menuButton=document.querySelector('.menu-toggle'),navigation=document.querySelector('#main-nav');menuButton?.addEventListener('click',()=>{const e=navigation.classList.toggle('open');menuButton.setAttribute('aria-expanded',e),menuButton.innerHTML=e?'Fermer <b>×</b>':'Menu <b>+</b>'});document.querySelectorAll('#main-nav a').forEach(e=>e.addEventListener('click',()=>{navigation.classList.remove('open'),menuButton?.setAttribute('aria-expanded','false'),menuButton&&(menuButton.innerHTML='Menu <b>+</b>')}));const observer=new IntersectionObserver(e=>e.forEach(e=>{e.isIntersecting&&(e.target.classList.add('visible'),observer.unobserve(e.target))}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
